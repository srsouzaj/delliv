import { Typography } from "../../components/Typography"

export const NotFoundScreen = () => {
    return (
        <div>
            <Typography>Essa página não foi encontrada</Typography>
        </div>
    )
}